# Database (for local development without Docker)
# Replace YOUR_USERNAME and YOUR_PASSWORD with your actual PostgreSQL credentials
DATABASE_URL=postgresql+psycopg://YOUR_USERNAME:YOUR_PASSWORD@localhost:5432/canada_basketball_ai
# For Docker: DATABASE_URL is set in docker-compose.local.yml

# Google Cloud Storage
GOOGLE_APPLICATION_CREDENTIALS=service-account.json
GCS_BUCKET_NAME=canada-basketball-scouting-reports

# LLM Configuration
# Recommended default: Cohere Command-A for general tasks
DEFAULT_MODEL=command-a-03-2025  # Default model

# Cohere API Key (required for default model)
COHERE_API_KEY=your_cohere_api_key_here

# Google AI API Key (for Gemini models - recommended for enhanced performance)
GOOGLE_API_KEY=your_google_api_key_here

# Model Configuration (per task - leave empty to use DEFAULT_MODEL)
ROUTER_MODEL=  # Fast intent classification (defaults to command-a-03-2025)
SQL_MODEL=  # SQL generation (defaults to provider default)
SCOUTING_MODEL=  # Scouting analysis (defaults to provider default)

# Application
ENVIRONMENT=development
LOG_LEVEL=INFO
PYTHONPATH=./
API_KEY=your_api_key_here

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173
