# Backend API URL (required)
# Local development (vite dev server)
VITE_BACKEND_URL=http://localhost:8080/api

# Production (Cloud Run)
# VITE_BACKEND_URL=https://your-backend-service-url.run.app/api
